<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:p="http://www.springframework.org/schema/p"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:mvc="http://www.springframework.org/schema/mvc" xmlns:aop="http://www.springframework.org/schema/aop"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
	http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd
	http://www.springframework.org/schema/mvc
	http://www.springframework.org/schema/mvc/spring-mvc.xsd">

    <context:component-scan base-package="com.aboluo.attend.controller" annotation-config="true"/>

    <!-- 设置默认配置方案 -->
    <mvc:annotation-driven/>
    <!-- 使用默认的Servlet来响应静态文件 -->
    <mvc:default-servlet-handler/>

    <!-- 视图解析器   -->
    <bean id="viewResolver"
          class="org.springframework.web.servlet.view.InternalResourceViewResolver"
          p:prefix="/WEB-INF/jsp/" p:suffix=".jsp"/>

    <!-- 静态资源映射 -->
    <mvc:resources mapping="/css/**" location="/static/assets/css/"></mvc:resources>
    <mvc:resources mapping="/js/**" location="/static/assets/js/"></mvc:resources>
    <mvc:resources mapping="/imgs/**" location="/static/assets/imgs/"></mvc:resources>
    <mvc:resources mapping="/fonts/**" location="/static/assets/fonts/"></mvc:resources>
    <mvc:resources mapping="/plugin/**" location="/static/assets/plugin/"></mvc:resources>

    <!-- 拦截器 -->
    <mvc:interceptors>
        <mvc:interceptor>
            <mvc:mapping path="/manager/**"/>
            <mvc:mapping path="/super/**"/>
            <mvc:mapping path="/attend/**"/>
            <mvc:exclude-mapping path="/manager/register"/>
            <mvc:exclude-mapping path="/login"/>
            <mvc:exclude-mapping path="/index.jsp"/>
            <bean class="com.aboluo.attend.interceptor.Interceptor"></bean>
        </mvc:interceptor>
    </mvc:interceptors>

    <!-- 文件上传 -->
    <bean id="multipartResolver" class="org.springframework.web.multipart.commons.CommonsMultipartResolver">
        <property name="maxUploadSize" value="10485760"/> <!-- 10m -->
        <property name="maxInMemorySize" value="4096" />
        <property name="defaultEncoding" value="UTF-8"/>
    </bean>

</beans>
